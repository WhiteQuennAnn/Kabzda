export default {
    title: "React memo demo",
}


const Filter = (props: any) => {
    return <div> {props.count}</div>
}

const Table = (props: any) => {
    return <div> {props.count}</div>
}
const Example = () => {

}
